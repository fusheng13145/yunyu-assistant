<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leyon.backend.mapper.UserMapper">

    <insert id="insert" parameterType="com.leyon.backend.entity.User">
        INSERT INTO app_user (id, name, password, assistant_ids, knowledge_ids)
        VALUES (#{id}, #{name}, #{password}, #{assistantIds}, #{knowledgeIds})
    </insert>

    <select id="selectByName" resultType="com.leyon.backend.entity.User">
        SELECT id, name, password, assistant_ids, knowledge_ids, created_at, updated_at
        FROM app_user
        WHERE name = #{name}
    </select>

    <select id="selectById" resultType="com.leyon.backend.entity.User">
        SELECT id, name, password, assistant_ids, knowledge_ids, created_at, updated_at
        FROM app_user
        WHERE id = #{id}
    </select>

</mapper>
